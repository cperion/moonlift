-- mlui_header.mlua
-- Compatibility module for the canonical MLUI LLPVM machine declaration.
-- Source of truth: mlui_stack.mlua.

local M = lalin.require("mlui_stack")

return {
    T = M.T,
    R = M.R,
    F = M.F,
    stack = M,
}
