return {
    activatable = require("ui.recipes.activatable"),
    selectable_list = require("ui.recipes.selectable_list"),
    reorderable_list = require("ui.recipes.reorderable_list"),
    edit_surface = require("ui.recipes.edit_surface"),
    scroll_view = require("ui.recipes.scroll_view"),
}
