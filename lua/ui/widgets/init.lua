return {
    button = require("ui.widgets.button"),
    toggle = require("ui.widgets.toggle"),
    switch = require("ui.widgets.toggle"),
    checkbox = require("ui.widgets.checkbox"),
    radio = require("ui.widgets.radio"),
    slider = require("ui.widgets.slider"),
    fader = require("ui.widgets.fader"),
    value_drag = require("ui.widgets.value_drag"),
    knob = require("ui.widgets.knob"),
    meter = require("ui.widgets.meter"),
    level = require("ui.widgets.meter"),
    progress = require("ui.widgets.progress"),
    list = require("ui.widgets.list"),
    listbox = require("ui.widgets.listbox"),
    tabs = require("ui.widgets.tabs"),
    popup = require("ui.widgets.popup"),
    overlay = require("ui.widgets.overlay"),
    tooltip = require("ui.widgets.tooltip"),
    popover = require("ui.widgets.popover"),
    modal = require("ui.widgets.modal"),
    dialog = require("ui.widgets.modal"),
    panel = require("ui.widgets.panel"),
    toolbar = require("ui.widgets.toolbar"),
    property_row = require("ui.widgets.property_row"),
    form_field = require("ui.widgets.form_field"),
    split_pane = require("ui.widgets.split_pane"),
    scroll_panel = require("ui.widgets.scroll_panel"),
    canvas = require("ui.widgets.canvas"),
    menu = require("ui.widgets.menu"),
    context_menu = require("ui.widgets.menu"),
    select = require("ui.widgets.select"),
    dropdown = require("ui.widgets.select"),
    text_input = require("ui.widgets.text_input"),
    text_area = require("ui.widgets.text_area"),
}
