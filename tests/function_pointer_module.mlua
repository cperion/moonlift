-- Function pointer test using Lua-first style

local callee = func(x: i32) -> i32
    return x + 1
end

local call_fp = func(fp: func(i32) -> i32, x: i32) -> i32
    return fp(x)
end

return { callee = callee, call_fp = call_fp }
