-- jit/opt_narrow.mlua -- Narrowing pass stub.

local m = module VmJitOptNarrow

type NarrowResult = narrow_done
    | abort(reason: i32)

region opt_narrow(J: ptr(u8), tr: i32) -> NarrowResult
entry start()
    jump narrow_done()
end
end

end

local _c = m:compile(); _c:free()
