-- jit/opt_narrow.mlua -- Narrowing pass stub.

local proto = moon.require("luajitvm.protocols")
local m = module "luajitvm.jit.opt_narrow"


region opt_narrow(J: ptr(u8), tr: i32) -> proto.NarrowResult
entry start()
    jump narrow_done()
end
end

end

local _c = m:compile(); _c:free()
