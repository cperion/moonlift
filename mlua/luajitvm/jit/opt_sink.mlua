-- jit/opt_sink.mlua -- Allocation sinking pass stub.
-- Protocol: SinkResult

local proto = moon.require("luajitvm.protocols")
local m = module "luajitvm.jit.opt_sink"


region opt_sink(J: ptr(u8), tr: i32) -> proto.SinkResult
entry start()
    jump disabled()
end
end

end

local _c = m:compile(); _c:free()
